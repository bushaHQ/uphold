import { TravelRuleWidget } from '@uphold/enterprise-travel-rule-widget-web-sdk';

// Expose the SDK class on the global scope so the HTML template can use it.
window.TravelRuleWidget = TravelRuleWidget;
